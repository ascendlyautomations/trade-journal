"use client"

import NativeIosBottomNav from "@/app/components/NativeIosBottomNav"

/**
 * Native iOS bottom tabs — existing NativeIosBottomNav (already web-no-op).
 */
export default function NativeIosBottomNavigation() {
  return <NativeIosBottomNav />
}
