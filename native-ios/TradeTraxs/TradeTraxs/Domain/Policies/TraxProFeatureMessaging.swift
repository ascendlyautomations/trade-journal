import Foundation

/// Shared user-facing copy for TraxPro gates — no external purchase steering.
nonisolated enum TraxProFeatureMessaging {
    static let featureTitle = "TraxPro feature"
    static let featureRequired = "This feature requires TraxPro."
    static let tradeAIUnavailable = "This feature requires TraxPro."
}
