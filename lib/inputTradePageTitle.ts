import { PAGE_HEADING_APP_CLASS } from "@/lib/pageHeadingStyles"

/** Shared layout + typography for the Input Trade page (`/app`) title row — single source of truth */
export const INPUT_TRADE_PAGE_TITLE_ROW_CLASSNAME =
  "w-full flex items-center justify-between mb-4"

/** Exact `<h1>` classes — preserve size and color when reusing */
export const INPUT_TRADE_PAGE_TITLE_CLASSNAME = PAGE_HEADING_APP_CLASS
